import React from 'react';

const Title = ({title}) => {
  return (
    <div>
      <h4 className=' text-[#FBA017] text-[14px] uppercase font-semibold'>{ title}</h4>
    </div>
  );
};

export default Title;