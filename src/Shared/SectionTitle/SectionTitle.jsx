import React from 'react';

const SectionTitle = ({title}) => {
  return (
    <div>
      <h2 className=' text-[#130929] font-bold text-[40px] text-start '>{ title}</h2>
    </div>
  );
};

export default SectionTitle;